.context-tools/
.venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
.motherduck-staging/

ingestion/
docs/
PROGRESS.md
CLAUDE.md
AGENTS.md
larkspur_ridge_bank_corpus/

# Holds real Langfuse credentials for the local langfuse-mcp server (same trust boundary as .env).
.mcp.json

# Raw import-lineage datasets (MotherDuck is the runtime source of truth; see context/README.md) — too large for git.
FinancialTransactionsDataset/
consumerComplaints/
consumerComplaints/

# Vendored Superpowers skill tooling, not this project's code.
.claude/skills/
.agents/

# Local git worktrees used for isolated plan execution.
.worktrees/

# Frontend build output and dependencies (frontend/.gitignore also covers these; kept here so a root-level check is enough).
node_modules/
frontend/dist/

# Machine-specific local tooling: absolute paths and the port-8001 workaround in launch.json; per-user Claude settings.
.claude/launch.json
.claude/settings.local.json

# Scratch space for one-off profiling scripts and throwaway output.
tmp/

# Tool caches and editor folders.
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.idea/
.vscode/
.DS_Store

# Local working notes for the assistant: session log and project instructions. Kept on disk, not in the repository.
PROGRESS.md
CLAUDE.md
